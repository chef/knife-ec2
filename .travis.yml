language: ruby
cache: bundler
sudo: false

branches:
  only:
    - master

before_install:
  - gem update --system
  - gem install bundler
  - bundle --version
  - gem --version

rvm:
  - 2.2.6
  - 2.3.3
  - 2.4.4
script: bundle exec rake spec
